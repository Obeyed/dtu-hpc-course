\begin{tikzpicture}
  \begin{axis}[
    ymajorgrids,
    xmajorgrids,
    xmin=65536,
    ylabel={Time (ms)},
    xlabel={Array size},
    xmode=log,
    log basis x={2},
    legend style={
      at={(0.05,0.95)},
      anchor=north west,
      column sep=1ex
     },
     no markers,
     very thick 
   ]

    \addplot table [x=x, y=parallel] {data/mapping.csv};
    \addlegendentry{parallel};
    \addplot+[dashed] table [x=x, y=sequential] {data/mapping.csv};
    \addlegendentry{sequential};
  \end{axis}
\end{tikzpicture}
