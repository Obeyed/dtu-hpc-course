\begin{tikzpicture}
  \begin{axis}[
    ymajorgrids,
    xmajorgrids,
    ylabel={Time (ms)},
    xlabel={Array size},
    xmode=log,
    log basis x={2},
    legend style={
      at={(0.05,0.95)},
      anchor=north west,
      column sep=1ex
     },
     no markers,
     very thick 
   ]

    \addplot table [x=x, y=parallel-global] {data/reduce.csv};
    \addlegendentry{parallel (global)};
    \addplot table [x=x, y=parallel-shared] {data/reduce.csv};
    \addlegendentry{parallel (shared)};
    \addplot+[dashed] table [x=x, y=sequential] {data/reduce.csv};
    \addlegendentry{sequential};
  \end{axis}
\end{tikzpicture}
